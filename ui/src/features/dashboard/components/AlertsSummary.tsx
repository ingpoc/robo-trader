import React from 'react'

/**
 * Legacy dashboard alerts panel.
 *
 * Overview is now driven by the operator snapshot and renders incidents inline,
 * so this component remains as a no-op placeholder until the old dashboard
 * surface is removed entirely.
 */
export const AlertsSummary: React.FC = () => null

export default AlertsSummary
